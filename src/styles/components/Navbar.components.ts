import styled from 'styled-components'

export const NavbarContainer = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4rem;
    flex-direction: row;
`

export const LogoTimeXp = styled.img`
    max-width: 30rem;
`

